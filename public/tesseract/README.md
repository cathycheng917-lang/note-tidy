# 本地 Tesseract 运行时文件

把以下文件放到本目录（最终会以 `/tesseract/...` 的路径被浏览器访问）：

- `worker.min.js`
- `tesseract-core.wasm.js`

来源（下载一次即可，运行时不再访问外网）：
- `worker.min.js`：来自 `tesseract.js` 的 dist
- `tesseract-core.wasm.js`：来自 `tesseract.js-core` 的发布文件

